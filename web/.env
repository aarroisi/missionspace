# Missionspace Frontend Environment Variables

# API endpoint (use proxy in development)
# Leave empty to use relative URLs which will go through Vite proxy
# VITE_API_URL=/api

# WebSocket endpoint (backend)
VITE_WS_URL=ws://localhost:4000/socket
